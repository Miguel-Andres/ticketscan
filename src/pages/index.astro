---
// Página principal de la aplicación
import Layout from '../layouts/Layout.astro';
---

<Layout title="Inicio" description="Sistema de procesamiento de etiquetas de envío con OCR">
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-blue-50 to-indigo-100 py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
          Facturas Generator Urban Logistic
        </h1>
        <p class="text-xl text-gray-600 mb-8 max-w-3xl mx-auto">
          Sistema automatizado para procesar etiquetas de envío usando OCR, 
          extraer datos importantes y generar informes PDF profesionales.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <button class="btn-primary text-lg px-8 py-3">
            Procesar Imágenes
          </button>
          <a href="/historial" class="btn-secondary text-lg px-8 py-3">
            Ver Historial
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="section-title">Características Principales</h2>
        <p class="text-gray-600 max-w-2xl mx-auto">
          Herramientas profesionales para optimizar el procesamiento de etiquetas de envío
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        <!-- Feature 1: OCR -->
        <div class="card text-center">
          <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mx-auto mb-4">
            <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
          </div>
          <h3 class="section-subtitle">OCR Avanzado</h3>
          <p class="text-gray-600">
            Extracción automática de datos de etiquetas usando Tesseract.js con alta precisión.
          </p>
        </div>

        <!-- Feature 2: PDF Generation -->
        <div class="card text-center">
          <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mx-auto mb-4">
            <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
          </div>
          <h3 class="section-subtitle">Generación PDF</h3>
          <p class="text-gray-600">
            Creación automática de informes PDF con datos extraídos y cálculos de precios.
          </p>
        </div>

        <!-- Feature 3: Batch Processing -->
        <div class="card text-center">
          <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mx-auto mb-4">
            <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
          </div>
          <h3 class="section-subtitle">Procesamiento por Lotes</h3>
          <p class="text-gray-600">
            Procesa múltiples imágenes simultáneamente para mayor eficiencia operativa.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Quick Start Section -->
  <section class="bg-gray-50 py-16">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="section-title">Cómo Usar el Sistema</h2>
        <p class="text-gray-600">
          Proceso simple en 3 pasos para procesar tus etiquetas de envío
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        <!-- Step 1 -->
        <div class="text-center">
          <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4 text-xl font-bold">
            1
          </div>
          <h3 class="font-semibold text-gray-900 mb-2">Subir Imágenes</h3>
          <p class="text-gray-600">
            Arrastra y suelta las imágenes de etiquetas o selecciona archivos desde tu dispositivo.
          </p>
        </div>

        <!-- Step 2 -->
        <div class="text-center">
          <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4 text-xl font-bold">
            2
          </div>
          <h3 class="font-semibold text-gray-900 mb-2">Revisar Datos</h3>
          <p class="text-gray-600">
            El OCR extrae automáticamente los datos. Revisa y edita si es necesario.
          </p>
        </div>

        <!-- Step 3 -->
        <div class="text-center">
          <div class="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center mx-auto mb-4 text-xl font-bold">
            3
          </div>
          <h3 class="font-semibold text-gray-900 mb-2">Generar PDF</h3>
          <p class="text-gray-600">
            Descarga el informe PDF con todos los datos procesados y precios calculados.
          </p>
        </div>
      </div>
    </div>
  </section>
</Layout>
