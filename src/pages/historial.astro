---
// Página de historial de procesamiento
import Layout from '../layouts/Layout.astro';
---

<Layout title="Historial" description="Historial de procesamiento de etiquetas de envío">
  <!-- Header Section -->
  <section class="bg-white border-b border-gray-200 py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center">
        <div>
          <h1 class="text-3xl font-bold text-gray-900">Historial de Procesamiento</h1>
          <p class="text-gray-600 mt-2">
            Consulta el historial de etiquetas procesadas y descarga informes anteriores
          </p>
        </div>
        <a href="/" class="btn-primary">
          Procesar Nuevas Imágenes
        </a>
      </div>
    </div>
  </section>

  <!-- Filters Section -->
  <section class="bg-gray-50 py-6">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <h2 class="section-subtitle mb-4">Filtros de Búsqueda</h2>
        
        <div class="grid md:grid-cols-4 gap-4">
          <!-- Filtro por fecha -->
          <div>
            <label for="date-from" class="block text-sm font-medium text-gray-700 mb-2">
              Fecha desde
            </label>
            <input 
              type="date" 
              id="date-from" 
              class="input-field"
              placeholder="Seleccionar fecha"
            />
          </div>
          
          <div>
            <label for="date-to" class="block text-sm font-medium text-gray-700 mb-2">
              Fecha hasta
            </label>
            <input 
              type="date" 
              id="date-to" 
              class="input-field"
              placeholder="Seleccionar fecha"
            />
          </div>

          <!-- Filtro por cliente -->
          <div>
            <label for="client-filter" class="block text-sm font-medium text-gray-700 mb-2">
              Número de Cliente
            </label>
            <input 
              type="text" 
              id="client-filter" 
              class="input-field"
              placeholder="Ej: 12345"
            />
          </div>

          <!-- Botón de búsqueda -->
          <div class="flex items-end">
            <button class="btn-primary w-full">
              Buscar
            </button>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Results Section -->
  <section class="py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Placeholder para cuando no hay datos -->
      <div class="text-center py-12">
        <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
          </svg>
        </div>
        <h3 class="text-lg font-medium text-gray-900 mb-2">
          No hay registros disponibles
        </h3>
        <p class="text-gray-600 mb-6">
          Aún no has procesado ninguna etiqueta. Comienza subiendo algunas imágenes.
        </p>
        <a href="/" class="btn-primary">
          Procesar Primera Etiqueta
        </a>
      </div>

      <!-- Ejemplo de tabla de resultados (para cuando haya datos) -->
      <div class="hidden">
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
          <div class="px-6 py-4 border-b border-gray-200">
            <h3 class="section-subtitle">Resultados de Búsqueda</h3>
          </div>
          
          <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Fecha
                  </th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Cliente
                  </th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Envío
                  </th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Localidad
                  </th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Estado
                  </th>
                  <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Acciones
                  </th>
                </tr>
              </thead>
              <tbody class="bg-white divide-y divide-gray-200">
                <!-- Ejemplo de fila -->
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    2024-01-15
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    12345
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    ENV-001
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    Buenos Aires
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800">
                      Procesado
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                    <button class="text-blue-600 hover:text-blue-900 mr-3">
                      Ver Detalles
                    </button>
                    <button class="text-green-600 hover:text-green-900">
                      Descargar PDF
                    </button>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Script para manejar filtros (placeholder para funcionalidad futura)
  document.addEventListener('DOMContentLoaded', function() {
    const searchButton = document.querySelector('button[class*="btn-primary"]');
    
    if (searchButton) {
      searchButton.addEventListener('click', function() {
        // Aquí se implementará la lógica de búsqueda
        console.log('Búsqueda activada - funcionalidad pendiente');
      });
    }
  });
</script>
