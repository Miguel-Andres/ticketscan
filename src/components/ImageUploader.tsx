import React from 'react';
import ImageUploaderComponent from './ImageUploader/index';

/**
 * Componente principal de carga de imágenes para OCR
 * Este componente ha sido refactorizado para mejorar la mantenibilidad
 * La implementación real se encuentra en ./ImageUploader/index.tsx
 */
const ImageUploader: React.FC = () => {
  return <ImageUploaderComponent />;
};

export default ImageUploader;
